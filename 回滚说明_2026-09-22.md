# 本次改动回滚说明（2026-09-22）

变更前快照：`回滚备份_变更前_2026-09-22.zip`

压缩包包含改动前的 `工程文件/src`、测试、文档、配置和 `成品`，未包含 `node_modules`、测试产物与构建缓存。

## 恢复步骤

1. 关闭开发服务器，并备份当前的 `工程文件` 与 `成品`。
2. 将压缩包解压到临时目录。
3. 用临时目录中的 `工程文件` 内容覆盖 `D:\Documents\抽卡规划小软件\工程文件`，再用临时目录中的 `成品` 覆盖 `D:\Documents\抽卡规划小软件\成品`。
4. 如需重新安装依赖，在 `工程文件` 执行 `npm ci`。

本次没有修改用户浏览器 IndexedDB 中的既有规划；若要恢复浏览器内数据，请在应用“设置”中导入改动前导出的 JSON 备份。
